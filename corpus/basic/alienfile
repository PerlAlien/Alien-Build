use alienfile;
use strict;
use warnings;

requires 'Foo', '1.00';

on 'configure' => sub {
  requires 'Early::Module', '1.234';
};

on 'share' => sub {
  requires 'Bar', '2.00';
};

on 'system' => sub {
  requires 'Baz', '3.00';
};

42;
